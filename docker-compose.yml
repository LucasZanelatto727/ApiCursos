services:
  postgres-db:
    image: postgres:latest
    container_name: meu-postgres-local
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=150699
      - POSTGRES_DB=cursos-api
    volumes:
      - postgres-data-local:/var/lib/postgresql/data
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U postgres" ]
      interval: 10s       # Tenta a cada 10 segundos
      timeout: 5s
      retries: 10         # Aumentamos as tentativas para 10
      start_period: 90s   # <-- A GRANDE MUDANÇA

  api-spring:
    container_name: api-cursos
    build: .
    ports:
      - "8080:8080"
    environment:
      - SPRING_DATASOURCE_URL=jdbc:postgresql://postgres-db:5432/cursos-api
      - SPRING_DATASOURCE_USERNAME=postgres
      - SPRING_DATASOURCE_PASSWORD=150699
    depends_on:
      postgres-db:
        condition: service_healthy

volumes:
  postgres-data-local:
