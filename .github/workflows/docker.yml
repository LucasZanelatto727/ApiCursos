name: CI/CD Docker build and Test

on:
  push:
    branches: [ "main" ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout do código
        uses: actions/checkout@v3

      - name: 🔧 Instalar o docker compose
        run: |
          sudo curl -L "https://github.com/docker/compose/releases/latest/download/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
          sudo chmod +x /usr/local/bin/docker-compose

      - name: 🚀 Build e Subida da Aplicação e do DB
        run: |
          # 1. Subir apenas o DB
          echo "Subindo o banco de dados..."
          docker compose up -d postgres-db

          # 2. Aguardar a porta 5432 do DB abrir usando o bash
          # --- CORREÇÃO AQUI: USAR 'localhost' ---
          DB_HOST="localhost" 
          DB_PORT="5432"
          MAX_WAIT=20 
          
          echo "Aguardando o serviço $DB_HOST:$DB_PORT estar acessível..."
          for i in $(seq 1 $MAX_WAIT); do
            # Tenta abrir uma conexão TCP com localhost:5432 (a porta mapeada)
            (exec 3<>/dev/tcp/$DB_HOST/$DB_PORT) 2>/dev/null
            if [ $? -eq 0 ]; then
              echo "✅ Banco de dados está pronto!"
              exec 3>&- 3<&-
              break
            fi
            sleep 1
            if [ $i -eq $MAX_WAIT ]; then
              echo "❌ O banco de dados não ficou pronto após $MAX_WAIT segundos."
              exit 1
            fi
          done
          
          # 3. Subir a API
          echo "Subindo a API-SPRING..."
          docker compose up -d --build api-spring

      - name: 🩺 Verificar status dos contêineres
        run: |
          echo "--- Status dos Contêineres ---"
          docker-compose ps
          # ... os logs continuam os mesmos ...

      - name: Aguardar e Verificar Health Check
        uses: ... # Use uma Action que possa fazer um HTTP GET e esperar um 200 OK
        env:
          HEALTH_URL: http://seu-servidor:8080/api/health

      - name: 🧪 Testar endpoint de saúde com Retry
        run: |
          MAX_RETRIES=20 # 20 tentativas * 5s = 100 segundos
          DELAY=5        
          URL="http://localhost:8080/api/health" # Aqui o localhost já estava correto
          
          echo "Testando endpoint de saúde da API..."
          
          for i in $(seq 1 $MAX_RETRIES); do
            echo "Tentativa $i/$MAX_RETRIES: Aguardando API responder em $URL"
            # O '-s' suprime o progresso, o '-f' faz o curl falhar em erros HTTP (4xx/5xx)
            if curl -s -f $URL; then
              echo "✅ Endpoint respondeu com sucesso!"
              exit 0 
            else
              sleep $DELAY
            fi
          done
          
          echo "❌ Endpoint de saúde não respondeu após $MAX_RETRIES tentativas."
          exit 1

      #- name: 📞 Realizar teste de saúde na API
      #  run: curl --fail http://localhost:8080/swagger-ui/index.html

      - name: 🧪 Testar endpoint de saúde
        run: |
          echo "Testando endpoint de saúde da API..."
          curl --fail -s http://localhost:8080/api/health || 
          (echo "❌ Endpoint de saúde não respondeu corretamente" && exit 1)
          echo "✅ Endpoint respondeu com sucesso!"

      - name: 🛑 Parar os contêineres após teste
        if: always()
        run: docker-compose down

      - name: ✅ Login no Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: 🚀 Build e Push da imagem para o Docker Hub
        uses: docker/build-push-action@v4
        with:
          context: .
          push: true
          tags: ${{ secrets.DOCKER_USERNAME }}/apicursos:latest

